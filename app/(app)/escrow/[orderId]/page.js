import { EscrowView } from "@/components/pages/escrow/EscrowView";

export const dynamic = "force-dynamic";

export const metadata = {
  robots: "noindex, nofollow",
};

export default function Page() {
  return <EscrowView />;
}
