import ReservePage from "@/components/pages/reserve/ReservePage";

export const dynamic = "force-dynamic";

export const metadata = {
  robots: "noindex, nofollow",
};

export default function Page() {
  return <ReservePage />;
}
